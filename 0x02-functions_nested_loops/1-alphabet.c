#include "main.h"

/**
 *  print_alphabet - prints in lowercase
 * Description: executes and prints
 * Returns:void
 */

void print_alphabet(void)
{
	char ch;
	for (ch = 'a'; ch <= 'z'; ch++)
		_putchar(ch);
	_putchar('\n');
}
