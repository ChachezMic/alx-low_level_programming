#include "main.h"
#include <string.h>
/**
 * _strlen - calculates the length of the string
 * @s:points to the string
 * Return:size of string
 */
int _strlen(char *s)
{
int i;

for (i = 0; s[i] != '\0'; i++)
{
	;
}
return (i);
}
